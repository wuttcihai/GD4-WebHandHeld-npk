import { Observable } from 'rxjs';
import { ModalConfig } from './modal-config';
export declare class ModalReference<TConfig, TResult = TConfig> {
    private _event;
    config: ModalConfig<TConfig>;
    constructor(config: ModalConfig<TConfig>);
    result(): Observable<TResult>;
    closeSuccess(model?: TResult): void;
    cancel(): void;
}
